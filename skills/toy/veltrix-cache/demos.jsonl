{"query": "Write the veltrix-cache call that stores feature payloads for batch.", "response": "Use the write path with every keyword argument set explicitly.\n\n```python\nvx_stash(\"batch\", key=key, payload=payload, ttl_s=30)\n```\n\nNote: ttl_s is mandatory and must not exceed 86400; Veltrix rejects a write without it.", "required": ["vx_stash", "batch"], "kind": "write"}
{"query": "Write the veltrix-cache call that reads back feature payloads from tier-one.", "response": "Use the read path against the same first argument.\n\n```python\nvx_fetch(\"tier-one\", key=key, payload=payload)\n```\n\nNote: ttl_s is mandatory and must not exceed 86400; Veltrix rejects a write without it.", "required": ["vx_fetch", "tier-one"], "kind": "read"}
{"query": "What does veltrix-cache raise when the bucket rule is violated, and what is the rule?", "response": "It raises `VX_TTL_MISSING`. The rule is that ttl_s is mandatory and must not exceed 86400; Veltrix rejects a write without it.", "required": ["VX_TTL_MISSING"], "kind": "rule"}
{"query": "Write the veltrix-cache call that stores pricing tables for shared.", "response": "Use the write path with every keyword argument set explicitly.\n\n```python\nvx_stash(\"shared\", key=key, payload=payload, ttl_s=250)\n```\n\nNote: ttl_s is mandatory and must not exceed 86400; Veltrix rejects a write without it.", "required": ["vx_stash", "shared"], "kind": "write"}
{"query": "Write the veltrix-cache call that reads back rendered thumbnails from eu-west-2.", "response": "Use the read path against the same first argument.\n\n```python\nvx_fetch(\"eu-west-2\", key=key, payload=payload)\n```\n\nNote: ttl_s is mandatory and must not exceed 86400; Veltrix rejects a write without it.", "required": ["vx_fetch", "eu-west-2"], "kind": "read"}
{"query": "What does veltrix-cache raise when the bucket rule is violated, and what is the rule?", "response": "It raises `VX_TTL_MISSING`. The rule is that ttl_s is mandatory and must not exceed 86400; Veltrix rejects a write without it.", "required": ["VX_TTL_MISSING"], "kind": "rule"}
{"query": "Write the veltrix-cache call that stores session tokens for batch.", "response": "Use the write path with every keyword argument set explicitly.\n\n```python\nvx_stash(\"batch\", key=key, payload=payload, ttl_s=250)\n```\n\nNote: ttl_s is mandatory and must not exceed 86400; Veltrix rejects a write without it.", "required": ["vx_stash", "batch"], "kind": "write"}
{"query": "Write the veltrix-cache call that reads back pricing tables from batch.", "response": "Use the read path against the same first argument.\n\n```python\nvx_fetch(\"batch\", key=key, payload=payload)\n```\n\nNote: ttl_s is mandatory and must not exceed 86400; Veltrix rejects a write without it.", "required": ["vx_fetch", "batch"], "kind": "read"}
{"query": "What does veltrix-cache raise when the bucket rule is violated, and what is the rule?", "response": "It raises `VX_TTL_MISSING`. The rule is that ttl_s is mandatory and must not exceed 86400; Veltrix rejects a write without it.", "required": ["VX_TTL_MISSING"], "kind": "rule"}
