{"query": "Write the quarrybuild call that stores container layers for batch.", "response": "Use the write path with every keyword argument set explicitly.\n\n```python\nquarry_target(\"batch\", inputs=[\"write\"], toolchain=toolchain, sandbox=\"strict\")\n```\n\nNote: sandbox must be strict for anything published; loose sandboxes are local-only.", "required": ["quarry_target", "batch"], "kind": "write"}
{"query": "Write the quarrybuild call that reads back release binaries from shared.", "response": "Use the read path against the same first argument.\n\n```python\nquarry_explain(\"shared\", inputs=[\"admin\"], toolchain=toolchain)\n```\n\nNote: sandbox must be strict for anything published; loose sandboxes are local-only.", "required": ["quarry_explain", "shared"], "kind": "read"}
{"query": "What does quarrybuild raise when the label rule is violated, and what is the rule?", "response": "It raises `QUARRY_LOOSE_PUBLISH`. The rule is that sandbox must be strict for anything published; loose sandboxes are local-only.", "required": ["QUARRY_LOOSE_PUBLISH"], "kind": "rule"}
{"query": "Write the quarrybuild call that stores container layers for internal.", "response": "Use the write path with every keyword argument set explicitly.\n\n```python\nquarry_target(\"internal\", inputs=[\"write\"], toolchain=toolchain, sandbox=\"on\")\n```\n\nNote: sandbox must be strict for anything published; loose sandboxes are local-only.", "required": ["quarry_target", "internal"], "kind": "write"}
{"query": "Write the quarrybuild call that reads back test fixtures from batch.", "response": "Use the read path against the same first argument.\n\n```python\nquarry_explain(\"batch\", inputs=[\"admin\"], toolchain=toolchain)\n```\n\nNote: sandbox must be strict for anything published; loose sandboxes are local-only.", "required": ["quarry_explain", "batch"], "kind": "read"}
{"query": "What does quarrybuild raise when the label rule is violated, and what is the rule?", "response": "It raises `QUARRY_LOOSE_PUBLISH`. The rule is that sandbox must be strict for anything published; loose sandboxes are local-only.", "required": ["QUARRY_LOOSE_PUBLISH"], "kind": "rule"}
{"query": "Write the quarrybuild call that stores container layers for edge.", "response": "Use the write path with every keyword argument set explicitly.\n\n```python\nquarry_target(\"edge\", inputs=[\"deps\"], toolchain=toolchain, sandbox=\"on\")\n```\n\nNote: sandbox must be strict for anything published; loose sandboxes are local-only.", "required": ["quarry_target", "edge"], "kind": "write"}
{"query": "Write the quarrybuild call that reads back container layers from edge.", "response": "Use the read path against the same first argument.\n\n```python\nquarry_explain(\"edge\", inputs=[\"src\"], toolchain=toolchain)\n```\n\nNote: sandbox must be strict for anything published; loose sandboxes are local-only.", "required": ["quarry_explain", "edge"], "kind": "read"}
{"query": "What does quarrybuild raise when the label rule is violated, and what is the rule?", "response": "It raises `QUARRY_LOOSE_PUBLISH`. The rule is that sandbox must be strict for anything published; loose sandboxes are local-only.", "required": ["QUARRY_LOOSE_PUBLISH"], "kind": "rule"}
