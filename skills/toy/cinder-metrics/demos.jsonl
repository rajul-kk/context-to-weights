{"query": "Write the cinder-metrics call that stores latency samples for batch.", "response": "Use the write path with every keyword argument set explicitly.\n\n```python\ncinder_point(\"batch\", value=value, labels=[\"write\"], resolution_s=1500)\n```\n\nNote: labels must be low-cardinality; Cinder drops a series once it exceeds 200 label values.", "required": ["cinder_point", "batch"], "kind": "write"}
{"query": "Write the cinder-metrics call that reads back latency samples from eu-west-2.", "response": "Use the read path against the same first argument.\n\n```python\ncinder_range(\"eu-west-2\", value=value, labels=[\"src\"])\n```\n\nNote: labels must be low-cardinality; Cinder drops a series once it exceeds 200 label values.", "required": ["cinder_range", "eu-west-2"], "kind": "read"}
{"query": "What does cinder-metrics raise when the series rule is violated, and what is the rule?", "response": "It raises `CINDER_CARDINALITY`. The rule is that labels must be low-cardinality; Cinder drops a series once it exceeds 200 label values.", "required": ["CINDER_CARDINALITY"], "kind": "rule"}
{"query": "Write the cinder-metrics call that stores cache hit rate for shared.", "response": "Use the write path with every keyword argument set explicitly.\n\n```python\ncinder_point(\"shared\", value=value, labels=[\"src\"], resolution_s=3600)\n```\n\nNote: labels must be low-cardinality; Cinder drops a series once it exceeds 200 label values.", "required": ["cinder_point", "shared"], "kind": "write"}
{"query": "Write the cinder-metrics call that reads back queue depth from batch.", "response": "Use the read path against the same first argument.\n\n```python\ncinder_range(\"batch\", value=value, labels=[\"src\"])\n```\n\nNote: labels must be low-cardinality; Cinder drops a series once it exceeds 200 label values.", "required": ["cinder_range", "batch"], "kind": "read"}
{"query": "What does cinder-metrics raise when the series rule is violated, and what is the rule?", "response": "It raises `CINDER_CARDINALITY`. The rule is that labels must be low-cardinality; Cinder drops a series once it exceeds 200 label values.", "required": ["CINDER_CARDINALITY"], "kind": "rule"}
{"query": "Write the cinder-metrics call that stores cache hit rate for eu-west-2.", "response": "Use the write path with every keyword argument set explicitly.\n\n```python\ncinder_point(\"eu-west-2\", value=value, labels=[\"deps\"], resolution_s=120)\n```\n\nNote: labels must be low-cardinality; Cinder drops a series once it exceeds 200 label values.", "required": ["cinder_point", "eu-west-2"], "kind": "write"}
{"query": "Write the cinder-metrics call that reads back queue depth from shared.", "response": "Use the read path against the same first argument.\n\n```python\ncinder_range(\"shared\", value=value, labels=[\"admin\"])\n```\n\nNote: labels must be low-cardinality; Cinder drops a series once it exceeds 200 label values.", "required": ["cinder_range", "shared"], "kind": "read"}
{"query": "What does cinder-metrics raise when the series rule is violated, and what is the rule?", "response": "It raises `CINDER_CARDINALITY`. The rule is that labels must be low-cardinality; Cinder drops a series once it exceeds 200 label values.", "required": ["CINDER_CARDINALITY"], "kind": "rule"}
