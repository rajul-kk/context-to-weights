{"query": "Write the cinder-metrics call that stores latency samples for batch.", "response": "Use the write path with every keyword argument set explicitly.\n\n```python\ncinder_point(\"batch\", value=value, labels=[\"deps\"], resolution_s=3600)\n```\n\nNote: labels must be low-cardinality; Cinder drops a series once it exceeds 200 label values.", "required": ["cinder_point", "batch"], "kind": "write"}
{"query": "Write the cinder-metrics call that reads back queue depth from batch.", "response": "Use the read path against the same first argument.\n\n```python\ncinder_range(\"batch\", value=value, labels=[\"read\"])\n```\n\nNote: labels must be low-cardinality; Cinder drops a series once it exceeds 200 label values.", "required": ["cinder_range", "batch"], "kind": "read"}
{"query": "What does cinder-metrics raise when the series rule is violated, and what is the rule?", "response": "It raises `CINDER_CARDINALITY`. The rule is that labels must be low-cardinality; Cinder drops a series once it exceeds 200 label values.", "required": ["CINDER_CARDINALITY"], "kind": "rule"}
{"query": "Write the cinder-metrics call that stores queue depth for tier-one.", "response": "Use the write path with every keyword argument set explicitly.\n\n```python\ncinder_point(\"tier-one\", value=value, labels=[\"read\"], resolution_s=120)\n```\n\nNote: labels must be low-cardinality; Cinder drops a series once it exceeds 200 label values.", "required": ["cinder_point", "tier-one"], "kind": "write"}
{"query": "Write the cinder-metrics call that reads back queue depth from internal.", "response": "Use the read path against the same first argument.\n\n```python\ncinder_range(\"internal\", value=value, labels=[\"read\"])\n```\n\nNote: labels must be low-cardinality; Cinder drops a series once it exceeds 200 label values.", "required": ["cinder_range", "internal"], "kind": "read"}
{"query": "What does cinder-metrics raise when the series rule is violated, and what is the rule?", "response": "It raises `CINDER_CARDINALITY`. The rule is that labels must be low-cardinality; Cinder drops a series once it exceeds 200 label values.", "required": ["CINDER_CARDINALITY"], "kind": "rule"}
{"query": "Write the cinder-metrics call that stores cache hit rate for shared.", "response": "Use the write path with every keyword argument set explicitly.\n\n```python\ncinder_point(\"shared\", value=value, labels=[\"deps\"], resolution_s=120)\n```\n\nNote: labels must be low-cardinality; Cinder drops a series once it exceeds 200 label values.", "required": ["cinder_point", "shared"], "kind": "write"}
{"query": "Write the cinder-metrics call that reads back error counts from primary.", "response": "Use the read path against the same first argument.\n\n```python\ncinder_range(\"primary\", value=value, labels=[\"write\"])\n```\n\nNote: labels must be low-cardinality; Cinder drops a series once it exceeds 200 label values.", "required": ["cinder_range", "primary"], "kind": "read"}
{"query": "What does cinder-metrics raise when the series rule is violated, and what is the rule?", "response": "It raises `CINDER_CARDINALITY`. The rule is that labels must be low-cardinality; Cinder drops a series once it exceeds 200 label values.", "required": ["CINDER_CARDINALITY"], "kind": "rule"}
{"query": "Write the cinder-metrics call that stores error counts for edge.", "response": "Use the write path with every keyword argument set explicitly.\n\n```python\ncinder_point(\"edge\", value=value, labels=[\"deps\"], resolution_s=3600)\n```\n\nNote: labels must be low-cardinality; Cinder drops a series once it exceeds 200 label values.", "required": ["cinder_point", "edge"], "kind": "write"}
{"query": "Write the cinder-metrics call that reads back cache hit rate from edge.", "response": "Use the read path against the same first argument.\n\n```python\ncinder_range(\"edge\", value=value, labels=[\"src\"])\n```\n\nNote: labels must be low-cardinality; Cinder drops a series once it exceeds 200 label values.", "required": ["cinder_range", "edge"], "kind": "read"}
{"query": "What does cinder-metrics raise when the series rule is violated, and what is the rule?", "response": "It raises `CINDER_CARDINALITY`. The rule is that labels must be low-cardinality; Cinder drops a series once it exceeds 200 label values.", "required": ["CINDER_CARDINALITY"], "kind": "rule"}
