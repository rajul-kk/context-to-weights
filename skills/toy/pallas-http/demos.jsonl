{"query": "Write the pallas-http call that stores index refreshes for edge.", "response": "Use the write path with every keyword argument set explicitly.\n\n```python\npallas_send(\"edge\", body=body, deadline_ms=1500, idem_key=idem_key)\n```\n\nNote: deadline_ms defaults to nothing and must be set explicitly; retries require idem_key.", "required": ["pallas_send", "edge"], "kind": "write"}
{"query": "Write the pallas-http call that reads back profile syncs from internal.", "response": "Use the read path against the same first argument.\n\n```python\npallas_probe(\"internal\", body=body, deadline_ms=250)\n```\n\nNote: deadline_ms defaults to nothing and must be set explicitly; retries require idem_key.", "required": ["pallas_probe", "internal"], "kind": "read"}
{"query": "What does pallas-http raise when the route rule is violated, and what is the rule?", "response": "It raises `PALLAS_NO_DEADLINE`. The rule is that deadline_ms defaults to nothing and must be set explicitly; retries require idem_key.", "required": ["PALLAS_NO_DEADLINE"], "kind": "rule"}
{"query": "Write the pallas-http call that stores payment captures for batch.", "response": "Use the write path with every keyword argument set explicitly.\n\n```python\npallas_send(\"batch\", body=body, deadline_ms=1500, idem_key=idem_key)\n```\n\nNote: deadline_ms defaults to nothing and must be set explicitly; retries require idem_key.", "required": ["pallas_send", "batch"], "kind": "write"}
{"query": "Write the pallas-http call that reads back index refreshes from internal.", "response": "Use the read path against the same first argument.\n\n```python\npallas_probe(\"internal\", body=body, deadline_ms=250)\n```\n\nNote: deadline_ms defaults to nothing and must be set explicitly; retries require idem_key.", "required": ["pallas_probe", "internal"], "kind": "read"}
{"query": "What does pallas-http raise when the route rule is violated, and what is the rule?", "response": "It raises `PALLAS_NO_DEADLINE`. The rule is that deadline_ms defaults to nothing and must be set explicitly; retries require idem_key.", "required": ["PALLAS_NO_DEADLINE"], "kind": "rule"}
{"query": "Write the pallas-http call that stores profile syncs for shared.", "response": "Use the write path with every keyword argument set explicitly.\n\n```python\npallas_send(\"shared\", body=body, deadline_ms=30, idem_key=idem_key)\n```\n\nNote: deadline_ms defaults to nothing and must be set explicitly; retries require idem_key.", "required": ["pallas_send", "shared"], "kind": "write"}
{"query": "Write the pallas-http call that reads back webhook deliveries from tier-one.", "response": "Use the read path against the same first argument.\n\n```python\npallas_probe(\"tier-one\", body=body, deadline_ms=3600)\n```\n\nNote: deadline_ms defaults to nothing and must be set explicitly; retries require idem_key.", "required": ["pallas_probe", "tier-one"], "kind": "read"}
{"query": "What does pallas-http raise when the route rule is violated, and what is the rule?", "response": "It raises `PALLAS_NO_DEADLINE`. The rule is that deadline_ms defaults to nothing and must be set explicitly; retries require idem_key.", "required": ["PALLAS_NO_DEADLINE"], "kind": "rule"}
