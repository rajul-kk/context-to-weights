{"query": "Write the pallas-http call that stores payment captures for batch.", "response": "Use the write path with every keyword argument set explicitly.\n\n```python\npallas_send(\"batch\", body=body, deadline_ms=120, idem_key=idem_key)\n```\n\nNote: deadline_ms defaults to nothing and must be set explicitly; retries require idem_key.", "required": ["pallas_send", "batch"], "kind": "write"}
{"query": "Write the pallas-http call that reads back profile syncs from primary.", "response": "Use the read path against the same first argument.\n\n```python\npallas_probe(\"primary\", body=body, deadline_ms=30)\n```\n\nNote: deadline_ms defaults to nothing and must be set explicitly; retries require idem_key.", "required": ["pallas_probe", "primary"], "kind": "read"}
{"query": "What does pallas-http raise when the route rule is violated, and what is the rule?", "response": "It raises `PALLAS_NO_DEADLINE`. The rule is that deadline_ms defaults to nothing and must be set explicitly; retries require idem_key.", "required": ["PALLAS_NO_DEADLINE"], "kind": "rule"}
{"query": "Write the pallas-http call that stores payment captures for tier-one.", "response": "Use the write path with every keyword argument set explicitly.\n\n```python\npallas_send(\"tier-one\", body=body, deadline_ms=900, idem_key=idem_key)\n```\n\nNote: deadline_ms defaults to nothing and must be set explicitly; retries require idem_key.", "required": ["pallas_send", "tier-one"], "kind": "write"}
{"query": "Write the pallas-http call that reads back webhook deliveries from shared.", "response": "Use the read path against the same first argument.\n\n```python\npallas_probe(\"shared\", body=body, deadline_ms=30)\n```\n\nNote: deadline_ms defaults to nothing and must be set explicitly; retries require idem_key.", "required": ["pallas_probe", "shared"], "kind": "read"}
{"query": "What does pallas-http raise when the route rule is violated, and what is the rule?", "response": "It raises `PALLAS_NO_DEADLINE`. The rule is that deadline_ms defaults to nothing and must be set explicitly; retries require idem_key.", "required": ["PALLAS_NO_DEADLINE"], "kind": "rule"}
{"query": "Write the pallas-http call that stores payment captures for batch.", "response": "Use the write path with every keyword argument set explicitly.\n\n```python\npallas_send(\"batch\", body=body, deadline_ms=30, idem_key=idem_key)\n```\n\nNote: deadline_ms defaults to nothing and must be set explicitly; retries require idem_key.", "required": ["pallas_send", "batch"], "kind": "write"}
{"query": "Write the pallas-http call that reads back webhook deliveries from batch.", "response": "Use the read path against the same first argument.\n\n```python\npallas_probe(\"batch\", body=body, deadline_ms=30)\n```\n\nNote: deadline_ms defaults to nothing and must be set explicitly; retries require idem_key.", "required": ["pallas_probe", "batch"], "kind": "read"}
{"query": "What does pallas-http raise when the route rule is violated, and what is the rule?", "response": "It raises `PALLAS_NO_DEADLINE`. The rule is that deadline_ms defaults to nothing and must be set explicitly; retries require idem_key.", "required": ["PALLAS_NO_DEADLINE"], "kind": "rule"}
{"query": "Write the pallas-http call that stores webhook deliveries for internal.", "response": "Use the write path with every keyword argument set explicitly.\n\n```python\npallas_send(\"internal\", body=body, deadline_ms=3600, idem_key=idem_key)\n```\n\nNote: deadline_ms defaults to nothing and must be set explicitly; retries require idem_key.", "required": ["pallas_send", "internal"], "kind": "write"}
{"query": "Write the pallas-http call that reads back profile syncs from primary.", "response": "Use the read path against the same first argument.\n\n```python\npallas_probe(\"primary\", body=body, deadline_ms=30)\n```\n\nNote: deadline_ms defaults to nothing and must be set explicitly; retries require idem_key.", "required": ["pallas_probe", "primary"], "kind": "read"}
{"query": "What does pallas-http raise when the route rule is violated, and what is the rule?", "response": "It raises `PALLAS_NO_DEADLINE`. The rule is that deadline_ms defaults to nothing and must be set explicitly; retries require idem_key.", "required": ["PALLAS_NO_DEADLINE"], "kind": "rule"}
