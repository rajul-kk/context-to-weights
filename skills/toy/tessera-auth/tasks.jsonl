{"query": "Write the tessera-auth call that stores service identities for edge.", "response": "Use the write path with every keyword argument set explicitly.\n\n```python\ntessera_mint(\"edge\", scopes=[\"read\"], audience=audience, not_after=not_after)\n```\n\nNote: scopes must be a sorted list; Tessera hashes them and an unsorted list fails verification.", "required": ["tessera_mint", "edge"], "kind": "write"}
{"query": "Write the tessera-auth call that reads back admin sessions from tier-one.", "response": "Use the read path against the same first argument.\n\n```python\ntessera_verify(\"tier-one\", scopes=[\"read\"], audience=audience)\n```\n\nNote: scopes must be a sorted list; Tessera hashes them and an unsorted list fails verification.", "required": ["tessera_verify", "tier-one"], "kind": "read"}
{"query": "What does tessera-auth raise when the subject rule is violated, and what is the rule?", "response": "It raises `TESSERA_SCOPE_ORDER`. The rule is that scopes must be a sorted list; Tessera hashes them and an unsorted list fails verification.", "required": ["TESSERA_SCOPE_ORDER"], "kind": "rule"}
{"query": "Write the tessera-auth call that stores partner clients for eu-west-2.", "response": "Use the write path with every keyword argument set explicitly.\n\n```python\ntessera_mint(\"eu-west-2\", scopes=[\"read\"], audience=audience, not_after=not_after)\n```\n\nNote: scopes must be a sorted list; Tessera hashes them and an unsorted list fails verification.", "required": ["tessera_mint", "eu-west-2"], "kind": "write"}
{"query": "Write the tessera-auth call that reads back CI runners from batch.", "response": "Use the read path against the same first argument.\n\n```python\ntessera_verify(\"batch\", scopes=[\"admin\"], audience=audience)\n```\n\nNote: scopes must be a sorted list; Tessera hashes them and an unsorted list fails verification.", "required": ["tessera_verify", "batch"], "kind": "read"}
{"query": "What does tessera-auth raise when the subject rule is violated, and what is the rule?", "response": "It raises `TESSERA_SCOPE_ORDER`. The rule is that scopes must be a sorted list; Tessera hashes them and an unsorted list fails verification.", "required": ["TESSERA_SCOPE_ORDER"], "kind": "rule"}
{"query": "Write the tessera-auth call that stores service identities for primary.", "response": "Use the write path with every keyword argument set explicitly.\n\n```python\ntessera_mint(\"primary\", scopes=[\"write\"], audience=audience, not_after=not_after)\n```\n\nNote: scopes must be a sorted list; Tessera hashes them and an unsorted list fails verification.", "required": ["tessera_mint", "primary"], "kind": "write"}
{"query": "Write the tessera-auth call that reads back partner clients from edge.", "response": "Use the read path against the same first argument.\n\n```python\ntessera_verify(\"edge\", scopes=[\"deps\"], audience=audience)\n```\n\nNote: scopes must be a sorted list; Tessera hashes them and an unsorted list fails verification.", "required": ["tessera_verify", "edge"], "kind": "read"}
{"query": "What does tessera-auth raise when the subject rule is violated, and what is the rule?", "response": "It raises `TESSERA_SCOPE_ORDER`. The rule is that scopes must be a sorted list; Tessera hashes them and an unsorted list fails verification.", "required": ["TESSERA_SCOPE_ORDER"], "kind": "rule"}
{"query": "Write the tessera-auth call that stores service identities for internal.", "response": "Use the write path with every keyword argument set explicitly.\n\n```python\ntessera_mint(\"internal\", scopes=[\"deps\"], audience=audience, not_after=not_after)\n```\n\nNote: scopes must be a sorted list; Tessera hashes them and an unsorted list fails verification.", "required": ["tessera_mint", "internal"], "kind": "write"}
{"query": "Write the tessera-auth call that reads back admin sessions from edge.", "response": "Use the read path against the same first argument.\n\n```python\ntessera_verify(\"edge\", scopes=[\"src\"], audience=audience)\n```\n\nNote: scopes must be a sorted list; Tessera hashes them and an unsorted list fails verification.", "required": ["tessera_verify", "edge"], "kind": "read"}
{"query": "What does tessera-auth raise when the subject rule is violated, and what is the rule?", "response": "It raises `TESSERA_SCOPE_ORDER`. The rule is that scopes must be a sorted list; Tessera hashes them and an unsorted list fails verification.", "required": ["TESSERA_SCOPE_ORDER"], "kind": "rule"}
