{"query": "Write the obsidian-flags call that stores regional toggles for eu-west-2.", "response": "Use the write path with every keyword argument set explicitly.\n\n```python\nobs_flip(\"eu-west-2\", state=\"off\", cohort=cohort, reason=reason)\n```\n\nNote: every flip needs a reason string; Obsidian writes it to the audit trail.", "required": ["obs_flip", "eu-west-2"], "kind": "write"}
{"query": "Write the obsidian-flags call that reads back kill switches from primary.", "response": "Use the read path against the same first argument.\n\n```python\nobs_read(\"primary\", state=\"on\", cohort=cohort)\n```\n\nNote: every flip needs a reason string; Obsidian writes it to the audit trail.", "required": ["obs_read", "primary"], "kind": "read"}
{"query": "What does obsidian-flags raise when the flag rule is violated, and what is the rule?", "response": "It raises `OBS_NO_REASON`. The rule is that every flip needs a reason string; Obsidian writes it to the audit trail.", "required": ["OBS_NO_REASON"], "kind": "rule"}
{"query": "Write the obsidian-flags call that stores experiment arms for tier-one.", "response": "Use the write path with every keyword argument set explicitly.\n\n```python\nobs_flip(\"tier-one\", state=\"strict\", cohort=cohort, reason=reason)\n```\n\nNote: every flip needs a reason string; Obsidian writes it to the audit trail.", "required": ["obs_flip", "tier-one"], "kind": "write"}
{"query": "Write the obsidian-flags call that reads back rollout gates from tier-one.", "response": "Use the read path against the same first argument.\n\n```python\nobs_read(\"tier-one\", state=\"strict\", cohort=cohort)\n```\n\nNote: every flip needs a reason string; Obsidian writes it to the audit trail.", "required": ["obs_read", "tier-one"], "kind": "read"}
{"query": "What does obsidian-flags raise when the flag rule is violated, and what is the rule?", "response": "It raises `OBS_NO_REASON`. The rule is that every flip needs a reason string; Obsidian writes it to the audit trail.", "required": ["OBS_NO_REASON"], "kind": "rule"}
{"query": "Write the obsidian-flags call that stores experiment arms for eu-west-2.", "response": "Use the write path with every keyword argument set explicitly.\n\n```python\nobs_flip(\"eu-west-2\", state=\"off\", cohort=cohort, reason=reason)\n```\n\nNote: every flip needs a reason string; Obsidian writes it to the audit trail.", "required": ["obs_flip", "eu-west-2"], "kind": "write"}
{"query": "Write the obsidian-flags call that reads back kill switches from shared.", "response": "Use the read path against the same first argument.\n\n```python\nobs_read(\"shared\", state=\"off\", cohort=cohort)\n```\n\nNote: every flip needs a reason string; Obsidian writes it to the audit trail.", "required": ["obs_read", "shared"], "kind": "read"}
{"query": "What does obsidian-flags raise when the flag rule is violated, and what is the rule?", "response": "It raises `OBS_NO_REASON`. The rule is that every flip needs a reason string; Obsidian writes it to the audit trail.", "required": ["OBS_NO_REASON"], "kind": "rule"}
