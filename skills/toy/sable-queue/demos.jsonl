{"query": "Write the sable-queue call that stores order events for edge.", "response": "Use the write path with every keyword argument set explicitly.\n\n```python\nsable_emit(\"edge\", envelope=envelope, partition_hint=partition_hint, dedupe_window_s=3600)\n```\n\nNote: partition_hint must be a stable string per entity, never a random value.", "required": ["sable_emit", "edge"], "kind": "write"}
{"query": "Write the sable-queue call that reads back order events from eu-west-2.", "response": "Use the read path against the same first argument.\n\n```python\nsable_drain(\"eu-west-2\", envelope=envelope, partition_hint=partition_hint)\n```\n\nNote: partition_hint must be a stable string per entity, never a random value.", "required": ["sable_drain", "eu-west-2"], "kind": "read"}
{"query": "What does sable-queue raise when the topic rule is violated, and what is the rule?", "response": "It raises `SABLE_UNSTABLE_HINT`. The rule is that partition_hint must be a stable string per entity, never a random value.", "required": ["SABLE_UNSTABLE_HINT"], "kind": "rule"}
{"query": "Write the sable-queue call that stores billing ticks for internal.", "response": "Use the write path with every keyword argument set explicitly.\n\n```python\nsable_emit(\"internal\", envelope=envelope, partition_hint=partition_hint, dedupe_window_s=250)\n```\n\nNote: partition_hint must be a stable string per entity, never a random value.", "required": ["sable_emit", "internal"], "kind": "write"}
{"query": "Write the sable-queue call that reads back device heartbeats from primary.", "response": "Use the read path against the same first argument.\n\n```python\nsable_drain(\"primary\", envelope=envelope, partition_hint=partition_hint)\n```\n\nNote: partition_hint must be a stable string per entity, never a random value.", "required": ["sable_drain", "primary"], "kind": "read"}
{"query": "What does sable-queue raise when the topic rule is violated, and what is the rule?", "response": "It raises `SABLE_UNSTABLE_HINT`. The rule is that partition_hint must be a stable string per entity, never a random value.", "required": ["SABLE_UNSTABLE_HINT"], "kind": "rule"}
{"query": "Write the sable-queue call that stores device heartbeats for primary.", "response": "Use the write path with every keyword argument set explicitly.\n\n```python\nsable_emit(\"primary\", envelope=envelope, partition_hint=partition_hint, dedupe_window_s=900)\n```\n\nNote: partition_hint must be a stable string per entity, never a random value.", "required": ["sable_emit", "primary"], "kind": "write"}
{"query": "Write the sable-queue call that reads back user signups from shared.", "response": "Use the read path against the same first argument.\n\n```python\nsable_drain(\"shared\", envelope=envelope, partition_hint=partition_hint)\n```\n\nNote: partition_hint must be a stable string per entity, never a random value.", "required": ["sable_drain", "shared"], "kind": "read"}
{"query": "What does sable-queue raise when the topic rule is violated, and what is the rule?", "response": "It raises `SABLE_UNSTABLE_HINT`. The rule is that partition_hint must be a stable string per entity, never a random value.", "required": ["SABLE_UNSTABLE_HINT"], "kind": "rule"}
