_base_: kaggle.yaml

run_root: /kaggle/working/artifacts/runs_qwen15

model:
  base: Qwen/Qwen2.5-1.5B-Instruct
  dtype: float16
  max_length: 1024

sleep:
  batch_size: 2
  grad_accum: 4

eval:
  batch_size: 8
